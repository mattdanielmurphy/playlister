{
	"devDependencies": {
		"@babel/cli": "^7.4.3",
		"@babel/core": "^7.4.3",
		"@babel/plugin-proposal-class-properties": "^7.4.0",
		"@babel/preset-env": "^7.4.3",
		"@babel/preset-react": "^7.0.0",
		"babel-watch": "^7.0.0",
		"concurrently": "^4.1.0",
		"rimraf": "^2.6.3"
	},
	"scripts": {
		"build": "babel server --out-dir functions --ignore server/node_modules --ignore server/client/node_modules",
		"set-build-folder": "if test -d build\nthen\nrm -r build\nfi&& mv server/client/build .",
		"build-client": "cd server/client && yarn build && cd ../../ && yarn set-build-folder",
		"build-modules": "babel server --out-dir functions --ignore server/node_modules --ignore server/client/node_modules",
		"prebuild-modules": "node node_modules/rimraf/bin functions",
		"postbuild-modules": "cd functions && yarn && cd client && yarn",
		"deploy": "yarn build && yarn build-client && firebase deploy",
		"dev": "concurrently \"nodemon server/index.js\" \"cd server/client && yarn start\""
	},
	"dependencies": {}
}
